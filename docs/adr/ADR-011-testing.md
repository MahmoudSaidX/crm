# ADR-011 --- Testing Strategy

**Status:** Accepted baseline.

## Decision

Use backend unit/integration tests, frontend unit/component tests and
architecture tests enforcing module boundaries; run reproducibly in CI.

## Rule

Story plans may refine details but must not silently contradict this
ADR.
